#!/bin/bash
# .githooks/pre-commit
# Universal enforcement layer — works for Codex, Cursor, Windsurf, Aider,
# and any agent (or human) that uses git commit.
#
# Activate: git config core.hooksPath .githooks
# Bypass (NOT recommended): git commit --no-verify
#
# Runs the same verification stack as Claude Code's stop-verify hook,
# plus the state-enforcement check against memory/progress.md.
#
# Configuration:
#   agent-md.toml [verify] typecheck / lint / test override heuristics.

set -u

FAIL=0
CHECKS=0

log() { printf '\033[36m▸\033[0m %s\n' "$*"; }
err() { printf '\033[31m✗\033[0m %s\n' "$*" >&2; }
ok()  { printf '\033[32m✓\033[0m %s\n' "$*"; }

log "Archimedes pre-commit verification"

# --- Minimal TOML reader (same logic as .claude/hooks/_lib.sh) ---
read_toml() {
  local file="$1" section="$2" key="$3"
  [ -f "$file" ] || return 0
  awk -v section="$section" -v key="$key" '
    BEGIN { in_sec = 0 }
    /^[[:space:]]*#/ { next }
    /^[[:space:]]*\[/ {
      sec = $0
      sub(/^[[:space:]]*\[/, "", sec); sub(/\][[:space:]]*$/, "", sec)
      gsub(/[[:space:]]/, "", sec)
      in_sec = (sec == section) ? 1 : 0
      next
    }
    in_sec && index($0, "=") > 0 {
      k = substr($0, 1, index($0, "=") - 1)
      v = substr($0, index($0, "=") + 1)
      gsub(/^[[:space:]]+|[[:space:]]+$/, "", k)
      sub(/[[:space:]]*#.*$/, "", v)
      gsub(/^[[:space:]]+|[[:space:]]+$/, "", v)
      if (k == key) {
        if (v ~ /^".*"$/)      { gsub(/^"|"$/, "", v) }
        else if (v ~ /^'\''.*'\''$/) { gsub(/^'\''|'\''$/, "", v) }
        print v
        exit
      }
    }
  ' "$file"
}

TOML="${AGENT_MD_TOML:-agent-md.toml}"
CFG_TYPECHECK=$(read_toml "$TOML" verify typecheck)
CFG_LINT=$(read_toml "$TOML" verify lint)
CFG_TEST=$(read_toml "$TOML" verify test)

run_check() {
  local label="$1" cmd="$2"
  [ -n "$cmd" ] || return 0
  CHECKS=$((CHECKS + 1))
  if eval "$cmd"; then
    ok "$label"
  else
    err "$label failed"
    FAIL=1
  fi
}

# --- Typecheck ---
if [ -n "$CFG_TYPECHECK" ]; then
  run_check "$CFG_TYPECHECK" "$CFG_TYPECHECK"
elif [ -f "tsconfig.json" ]; then
  run_check "tsc --noEmit" "npx tsc --noEmit"
fi

# --- Lint ---
if [ -n "$CFG_LINT" ]; then
  run_check "$CFG_LINT" "$CFG_LINT"
elif ls .eslintrc* eslint.config.* 2>/dev/null | grep -q .; then
  run_check "eslint" "npx eslint . --quiet"
fi

# --- Python ruff (when no explicit lint override) ---
if [ -z "$CFG_LINT" ] && command -v ruff &>/dev/null; then
  HAS_PY=$(git diff --cached --name-only | grep -c '\.py$' || true)
  if [ "${HAS_PY:-0}" -gt 0 ] || ls *.py 2>/dev/null | head -1 | grep -q .; then
    run_check "ruff" "ruff check ."
  fi
fi

# --- Python mypy ---
if [ -z "$CFG_TYPECHECK" ] && command -v mypy &>/dev/null \
   && { [ -f "mypy.ini" ] || grep -q '\[tool.mypy\]' pyproject.toml 2>/dev/null; }; then
  run_check "mypy" "mypy ."
fi

# --- Rust ---
if [ -z "$CFG_TYPECHECK" ] && [ -f "Cargo.toml" ]; then
  run_check "cargo check" "cargo check"
fi

# --- Tests ---
if [ -n "$CFG_TEST" ]; then
  run_check "$CFG_TEST" "$CFG_TEST"
elif [ -f "package.json" ]; then
  # Detect package manager via lockfile (pnpm > yarn > bun > npm)
  PM="npm"
  [ -f "pnpm-lock.yaml" ] && PM="pnpm"
  [ -f "yarn.lock" ] && PM="yarn"
  [ -f "bun.lockb" ] || [ -f "bun.lock" ] && PM="bun"
  HAS_TEST=$(node -e "try{const p=require('./package.json');if(p.scripts&&p.scripts.test&&p.scripts.test!=='echo \"Error: no test specified\" && exit 1')process.stdout.write('1')}catch{}" 2>/dev/null)
  if [ "${HAS_TEST:-}" = "1" ]; then
    case "$PM" in
      pnpm) run_check "pnpm test" "pnpm test --silent" ;;
      yarn) run_check "yarn test" "yarn test --silent" ;;
      bun)  run_check "bun test"  "bun test" ;;
      npm)  run_check "npm test"  "npm test --silent" ;;
    esac
  fi
elif [ -f "pyproject.toml" ] || [ -f "pytest.ini" ]; then
  if command -v pytest &>/dev/null; then
    run_check "pytest" "pytest --tb=short -q"
  fi
elif [ -f "Cargo.toml" ]; then
  run_check "cargo test" "cargo test --quiet"
fi

# --- State enforcement (memory/progress.md) ---
if [ -f "memory/progress.md" ]; then
  STAGED=$(git diff --cached --name-only)
  SOURCE_CHANGED=$(
    echo "$STAGED" \
      | grep -vE '^(memory/|docs/|\.claude/|\.githooks/|skills/|README\.md$|LICENSE$|AGENT\.md$|AGENTS\.md$|CLAUDE\.md$|\.cursorrules$|\.windsurfrules$|CONVENTIONS\.md$|agent-md\.toml(\.example)?$)' \
      | grep -v '^$' \
      | grep -cvE '\.md$' || true
  )
  PROGRESS_STAGED=$(echo "$STAGED" | grep -c '^memory/progress\.md$' || true)

  if [ "${SOURCE_CHANGED:-0}" -gt 0 ] && [ "${PROGRESS_STAGED:-0}" -eq 0 ]; then
    err "memory/progress.md not staged — ${SOURCE_CHANGED} source file(s) changed without progress update"
    FAIL=1
  else
    ok "progress.md up to date"
  fi
fi

echo ""
if [ "$FAIL" -eq 1 ]; then
  err "Pre-commit BLOCKED. Fix errors above, re-stage, and retry."
  echo "   (Bypass not recommended: git commit --no-verify)"
  exit 1
fi

if [ "$CHECKS" -eq 0 ]; then
  log "No type-checker, linter, or test suite detected. Commit allowed but unverified."
  log "Declare commands in agent-md.toml to make this deterministic."
else
  ok "All $CHECKS check(s) passed."
fi
exit 0
